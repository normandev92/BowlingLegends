@model IEnumerable<BowlingLegends.Models.Season>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinalDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinalDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SeasonID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SeasonID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SeasonID })
        </td>
    </tr>
}

</table>
